{
  "name": "nestack-technologies",
  "version": "1.0.0",
  "description": "This project implements a context-aware rate limiting middleware using Node.js and Express. It enforces different rate limits based on the user's tier (`free` or `paid`) and the type of endpoint they are accessing (`ai` or `read`).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  }
}
